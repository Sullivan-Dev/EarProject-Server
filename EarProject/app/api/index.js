const contacts = require('./contacts');
const me = require('./me');
const services = require('./services');
const sessions = require('./sessions');

module.exports = {
  contacts,
  me,
  services,
  sessions
}